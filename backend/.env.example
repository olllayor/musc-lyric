# APPLICATION key from https://acoustid.org/new-application (used as `client` param).
# NOTE: this is NOT the personal "user API key" on your profile page — that one
# only works for submits and lookups will reject it with "invalid API key".
ACOUSTID_API_KEY=your_app_key_here
